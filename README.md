# What is okta-dac-docs?
Repo for [okta-dac](https://github.com/oktadeveloper/okta-dac) Documentation

## Local development
* `cd` into `/okta-dac-docs` directory
* Run `npm install`
* Run `npm run dev` to serve locally
* Open a browser to `localhost:8082` 
